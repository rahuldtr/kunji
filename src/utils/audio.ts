// Audio disabled per user preference ("no sound in app")
export const SoundEffects = {
  playBloom(_mood?: 'happy' | 'neutral' | 'sad') {
    // Silence - no audio playback
  },
  playWaterSplash() {
    // Silence - no audio playback
  },
};
